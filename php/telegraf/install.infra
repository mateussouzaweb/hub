infralet only_sudo

# Add PHP-FPM
infralet ask_yes_no \
    TELEGRAF_INPUT_PHP_FPM \
    "N" \
    "Do you want to input PHP-FPM on Telegraf?"

if [ $TELEGRAF_INPUT_PHP_FPM == "Y" ]; then
    infralet copy php-fpm.conf /etc/telegraf/telegraf.d/php-fpm.conf
fi