[smtp]
enabled = true
host = ${GRAFANA_SMTP_HOST}:${GRAFANA_SMTP_PORT}
user = ${GRAFANA_SMTP_USERNAME}
password = ${GRAFANA_SMTP_PASSWORD}
from_address = ${GRAFANA_EMAIL}
from_name = Grafana Alerts
ehlo_identity = ${GRAFANA_SMTP_HOST}